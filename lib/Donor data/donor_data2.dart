import 'package:flutter/material.dart';

class donorData2 extends StatelessWidget {
  const donorData2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
